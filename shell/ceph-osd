#!/bin/bash

    docker run -d \
    --name=osd \
    --net=host \
    --restart=always \
    --privileged=true \
    --pid=host \
    -v /ceph/etc:/etc/ceph \
    -v /var/lib/ceph:/var/lib/ceph \
    -v /var/log/ceph:/var/log/ceph \
    ceph/daemon osd_directory
